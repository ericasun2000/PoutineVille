<% include partials/_header %>

    <div class="container">
      <div class="display-container mt-5 mx-auto p-5">
      <h3 class="mt-3">Edit Dish</h3>

      <form method="POST" action="/admin/display/<%= dish.id %>/edit">
        <div class="form-group row">
          <label for="dishName" class="col-sm-2 col-form-label">Name</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="dishName" name="updatedDish[name]" placeholder="name" value="<%= dish.name %>">
          </div>
        </div>
        <div class="form-group row">
          <label for="dishDescription" class="col-sm-2 col-form-label">Description</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="dishDescription" name="updatedDish[description]" placeholder="description" value="<%= dish.description %>">
          </div>
        </div>
        <div class="form-group row">
          <label for="dishPrice" class="col-sm-2 col-form-label">Price</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="dishPrice" name="updatedDish[price]" placeholder="price" value="<%= dish.price %>">
          </div>
        </div>
        <div class="form-group row">
          <label for="dishUrl" class="col-sm-2 col-form-label">Url</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="dishUrl" name="updatedDish[image_url]" placeholder="image_url" value="<%= dish.image_url %>">
          </div>
        </div>

        <button type="submit" class="btn btn-outline-secondary">Update Dish</button>
      </form>

  <% include partials/_footer %>
